- name: value 10 and condition expects greather than equal to 15
  hosts: local
  vars:
   Sessions: 10
  tasks:
   - name: checking condition based on int
     debug:
       msg: " Sai is elgible for next class"
     when: Sessions >= 15


- name: value 10 and condition expects greather than equal to 5
  hosts: local
  vars:
   Sessions: 10
  tasks:
   - name: checking condition based on int
     debug:
       msg: " Sai is elgible for next class"
     when: Sessions >= 5

- name: value chennai condition expects chennai
  hosts: local
  vars:
    place: "Chennai"
  tasks:
   - name: checking condition based on string
     debug:
       msg: " only prints when place is exact Chennai"
     when: place == "Chennai"

- name: value chennai condition expects CHENNAI
  hosts: local
  vars:
    place: "Chennai"
  tasks:
   - name: checking condition based on string
     debug:
       msg: " only prints when place is exact CHENNAI"
     when: place == "CHENNAI"
